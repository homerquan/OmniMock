# Contributing

Read `AGENTS.md` and the relevant sections of `SPEC.md` before changing
architecture or user-visible behavior. Keep domain code framework-independent,
add deterministic tests with behavior changes, update declarative schemas and
samples together, and report the exact validation commands run.
